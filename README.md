# Objecet-detection-training


To train this model download pretrained YOLOv3 model and convert the model to keras model using convert.py then run train.py
I used OIDV4 tool kit to download datasets.

